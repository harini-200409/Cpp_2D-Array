#include <iostream>
using namespace std;
int main()
{
    int r,c;
    cin>>r>>c;
    if(r!=c)
    {
        cout<<"Not symmetric";
        return 0;
    }
    int mat[r][c];
    for(int i=0;i<r;i++)
    {
        for(int j=0;j<c;j++)
        {
            cin>>mat[i][j];
        }
    }
    int sym=1;
    for(int i=0;i<r;i++)
    {
        for(int j=0;j<c;j++)
        {
            if(mat[i][j]!=mat[j][i])
            {
                sym=0;
                break;
                goto l1;
            }
        }
    }
    l1:
    if(sym)
    {
        cout<<"symmetric";
    }
    else
    {
        cout<<"Not symmetric";
    }
    return 0;
}
